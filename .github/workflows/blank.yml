name: CI

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Generate cmake 
      run: |
        echo Start generate CMake
        call generate.cmd
